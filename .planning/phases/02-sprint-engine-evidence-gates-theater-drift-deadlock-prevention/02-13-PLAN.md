---
phase: 02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention
plan: 13
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12]
files_modified:
  - tests/test_phase2_integration.py
  - tests/test_template_regression_matrix.py
autonomous: false
requirements: [CORE-05, CORE-07, INT-06, SPRINT-01, SPRINT-02, SKILL-09, SAFETY-01, SAFETY-02, SAFETY-03, SAFETY-04, QUALITY-01, QUALITY-02, QUALITY-03, QUALITY-06, QUALITY-08, QUALITY-11, UX-02, UX-03, UX-04, UX-05, UX-09]
requirements_addressed: [CORE-05, CORE-07, INT-06, SPRINT-01, SPRINT-02, SKILL-09, SAFETY-01, SAFETY-02, SAFETY-03, SAFETY-04, QUALITY-01, QUALITY-02, QUALITY-03, QUALITY-06, QUALITY-08, QUALITY-11, UX-02, UX-03, UX-04, UX-05, UX-09]
tags: [phase-2-regression, phase-0-bc-gate, end-to-end, integration, sc-10, ship-gate]
autofix: [ruff-format, ruff-check-fix]

must_haves:
  truths:
    - "Phase 0 regression matrix tests/test_template_regression_matrix.py passes 12/12 across all 6 packaged templates (software-dev, hedge-fund, code-review, harness-default, research-paper, strategy-room) — SC#10, Pitfall #8 invariant, BLOCKING for Phase 2 ship"
    - "New end-to-end integration test tests/test_phase2_integration.py exercises: start sprint → write evidence artifact → advance past EvidenceGate → pause → resume → status via CLI → list via CLI → JSON envelope verification — proves the 21 REQ-IDs hang together"
    - "Full pytest suite (pytest tests/ -q) passes — zero regressions across Phase 0, Phase 1, Phase 2 test files"
    - "All 21 Phase 2 REQ-IDs have at least one dedicated passing test in the phase plan set — verified by a traceability grep over the 10+ new test files"
    - "Ruff clean across all Phase 2-modified files (clawteam/sprint/*, clawteam/harness/evidence_*.py, clawteam/harness/forced_progress_gate.py, clawteam/harness/freeze_registry.py, clawteam/team/envelope.py, clawteam/team/routing_policy.py, clawteam/transport/base.py, clawteam/cli/commands.py, clawteam/events/types.py, clawteam/plugins/{base,manager}.py, clawteam/mcp/{server,helpers}.py, clawteam/workspace/manager.py, clawteam/harness/artifacts.py, clawteam/harness/errors.py)"
    - "Human-verify checkpoint confirms the Phase 2 ship criteria from ROADMAP §Phase 2 Success Criteria #1..#10 are all met — 10 numbered items"
  artifacts:
    - path: "tests/test_phase2_integration.py"
      provides: "End-to-end smoke test: sprint lifecycle across all Phase 2 primitives — start → evidence → advance → pause → resume → CLI status → CLI list → JSON envelope"
      contains: "test_full_sprint_lifecycle_end_to_end"
      min_lines: 180
    - path: "tests/test_template_regression_matrix.py"
      provides: "Phase 0 regression matrix — unchanged from Phase 0; this plan ASSERTS it still passes 12/12 (no edits expected here unless a Phase 2 change surfaced a hidden BC violation, in which case fix the offending Phase 2 code, not this file)"
      contains: "test_template_spawn_and_advance"
  key_links:
    - from: "tests/test_phase2_integration.py::test_full_sprint_lifecycle_end_to_end"
      to: "clawteam/sprint/conductor.py::SprintConductor + all Phase 2 primitives"
      via: "One test exercises: SprintConductor → EvidenceGate → forced_progress_gate → InteractionGate → FreezeRegistry → CLI sub-app → JSON envelope in a single flow"
      pattern: "test_full_sprint_lifecycle_end_to_end"
    - from: "tests/test_template_regression_matrix.py"
      to: "Phase 0 Plan 00-04-SUMMARY.md baseline"
      via: "Baseline 12/12 test count must hold after Phase 2 ships (SC#10)"
      pattern: "test_template_spawn_and_advance"

user_setup: []
---

<objective>
The Phase 2 ship-gate plan. This plan contains two mandatory gates and one human-verify
checkpoint that together determine whether Phase 2 is done:

1. **Phase 0 regression matrix hard-green gate (SC#10, Pitfall #8, BLOCKING)** — re-run
   `pytest tests/test_template_regression_matrix.py -q`. All 12 cases across 6 templates
   must pass. If any regression surfaces, STOP, identify the Phase 2 plan responsible,
   fix at source (not here). This gate enforces the upstream-PR viability invariant:
   every new Phase 2 primitive is opt-in through `SprintConductor` or
   `GstackSprintPlugin`; existing templates keep their original `ArtifactRequiredGate`,
   `DefaultRoutingPolicy`, `EventBus` subscribers, and `TeamMessage` semantics.

2. **Phase 2 end-to-end integration test (new)** — one test that wires the full sprint
   lifecycle across every Phase 2 primitive, proving the 21 REQ-IDs hang together:
   start sprint via CLI → write an evidence-bearing artifact → advance through
   EvidenceGate → verify forced_progress_gate + envelope enforcement do not false-fire
   → pause → resume → query status via CLI → list via CLI → assert JSON envelope shape
   uniform. Not a stress test (10-concurrent is Phase 7); just the canonical happy path.

3. **Human-verify checkpoint** — user confirms the 10 ROADMAP §Phase 2 Success Criteria
   are functionally met by reading the Phase 2 SUMMARY chain and running the CLI
   against a fresh test fixture. Pass = Phase 2 ready to merge.

Output:
- `tests/test_phase2_integration.py` (new, ~200 LOC, 3-5 end-to-end tests).
- Regression-matrix assertion (invocation only; no edit).
- Checkpoint task gating Phase 2 completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-CONTEXT.md
@.planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-VALIDATION.md
@.planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-01-PLAN.md
@.planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-11-PLAN.md
@.planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-12-PLAN.md
@tests/test_template_regression_matrix.py
@clawteam/sprint/conductor.py
@clawteam/cli/commands.py

<interfaces>
<!-- All Phase 2 primitives converge here; interfaces are the sum of Plans 02-01..02-12. -->

<!-- From Plan 02-11 SprintConductor API + Plan 02-12 CLI -->
```python
class SprintConductor:
    def start_sprint(self, goal: str, auto_advance: bool = True) -> SprintState: ...
    def advance_phase(self, sprint_id: str) -> tuple[bool, str]: ...
    def pause(self, sprint_id: str) -> SprintState: ...
    def resume(self, sprint_id: str) -> SprintState: ...

# CLI surface:
# clawteam sprint start --team T --goal "..."
# clawteam sprint status <id|prefix> --team T
# clawteam sprint pause <id|prefix> --team T
# clawteam sprint resume <id|prefix> --team T
# clawteam sprint list --team T
# clawteam sprint show <id|prefix> --team T
# All with --json → uniform {ok, data, warnings, error}
```

<!-- From Plan 02-04 EvidenceSchemaRegistry + Plan 02-07 EvidenceGate -->
```python
class ArtifactFrontmatterBase(BaseModel):
    persona: str; step_label: str; done: bool; artifact_type: str; created_at: str

def register_schema(name: str, cls: type[ArtifactFrontmatterBase]) -> None: ...
def reset_registry() -> None: ...

class EvidenceGate(ArtifactRequiredGate):
    def check(self, state) -> tuple[bool, str]: ...  # 4-layer protocol
```

<!-- Phase 0 regression matrix — PRESERVE UNCHANGED -->
```python
# tests/test_template_regression_matrix.py (Phase 0 Plan 00-04):
# 12 parametrized cases spawning + advancing each of 6 templates through its
# existing phase sequence; every case must pass unchanged after Phase 2 ships.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 2 end-to-end integration test</name>
  <files>tests/test_phase2_integration.py</files>
  <read_first>
    - tests/test_sprint_cli.py (Plan 02-12 CliRunner patterns + hermetic env fixture)
    - tests/test_sprint_conductor.py (Plan 02-11 conductor fixtures)
    - tests/test_evidence_gate.py (Plan 02-07 fixture for registering a DesignDocFixture)
    - clawteam/sprint/conductor.py (SprintConductor API)
    - clawteam/cli/commands.py (sprint_app — invoked via CliRunner in JSON mode)
    - clawteam/harness/evidence_schemas.py (register_schema + reset_registry for the test)
    - .planning/ROADMAP.md §Phase 2 Success Criteria (the 10-item checklist the test validates)
    - .planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-CONTEXT.md §D-01..D-29 (full decision map)
  </read_first>
  <action>
Create `tests/test_phase2_integration.py`:

```python
"""Phase 2 end-to-end integration — one test wires the full sprint lifecycle
across every Phase 2 primitive, proving the 21 REQ-IDs hang together.

Coverage:
- Sprint start via CLI (UX-02)
- Evidence artifact write + frontmatter validation (SPRINT-02, QUALITY-08, SKILL-09)
- Sprint pause survives process-level checkpoint write (CORE-07)
- Sprint resume restores state + re-emits PhaseTransition (CORE-07)
- CLI status/show/list with uniform JSON envelope (UX-03, UX-04, UX-05, UX-09)
- Freeze registry sprint-internal exemption (SAFETY-02, Pitfall #5)
- forced_progress_gate does NOT false-fire for a well-behaved turn (QUALITY-11)
- Envelope enforcement scoped properly (QUALITY-01, Pitfall #8 BC)
"""
from __future__ import annotations
import json
from pathlib import Path

import pytest
from pydantic import Field
from typer.testing import CliRunner

runner = CliRunner()


@pytest.fixture
def e2e_env(tmp_path, monkeypatch):
    """Hermetic env for end-to-end test."""
    monkeypatch.setenv("CLAWTEAM_DATA_DIR", str(tmp_path))
    monkeypatch.delenv("CLAWTEAM_TEAM", raising=False)
    # Reset all module-level singleton state
    from clawteam.harness.evidence_schemas import reset_registry
    from clawteam.harness.freeze_registry import reset_freeze_registry, reset_safety_subscribers
    reset_registry()
    reset_freeze_registry()
    reset_safety_subscribers()
    yield tmp_path
    reset_registry()
    reset_freeze_registry()
    reset_safety_subscribers()


def test_full_sprint_lifecycle_end_to_end(e2e_env):
    """Canonical happy path across Phase 2 primitives."""
    from clawteam.cli.commands import app

    # ── Step 1: register a minimal DesignDoc evidence schema ───────────────
    from clawteam.harness.evidence_schemas import ArtifactFrontmatterBase, register_schema

    class DesignDocSchema(ArtifactFrontmatterBase):
        artifact_type: str = "design-doc"

    register_schema("design-doc", DesignDocSchema)

    # ── Step 2: start sprint via CLI (UX-02, JSON envelope UX-09) ───────────
    result = runner.invoke(app, [
        "--json", "sprint", "start",
        "--team", "e2e", "--goal", "verify phase 2 integration",
    ])
    assert result.exit_code == 0, result.output
    start_payload = json.loads(result.output)
    assert start_payload["ok"] is True
    assert start_payload["error"] is None
    sid = start_payload["data"]["id"]
    assert len(sid) == 8

    # state.json exists on disk (CORE-05 + CORE-04 persistence)
    state_path = e2e_env / "teams/e2e/sprints" / sid / "state.json"
    assert state_path.exists()
    raw = json.loads(state_path.read_text())
    assert raw["status"] == "running"
    assert raw["goal"] == "verify phase 2 integration"

    # ── Step 3: status via CLI (UX-03) ──────────────────────────────────────
    result = runner.invoke(app, ["--json", "sprint", "status", sid, "--team", "e2e"])
    assert result.exit_code == 0, result.output
    status_payload = json.loads(result.output)
    assert status_payload["ok"] is True
    assert status_payload["data"]["pending_questions_count"] == 0
    assert status_payload["data"]["most_recent_artifact"] == ""

    # ── Step 4: show via CLI (UX-05) ────────────────────────────────────────
    result = runner.invoke(app, ["--json", "sprint", "show", sid, "--team", "e2e"])
    assert result.exit_code == 0, result.output
    show_payload = json.loads(result.output)
    expected = {"sprint_id", "goal", "current_phase", "status", "phase_history",
                "artifacts_list", "auto_advance", "team", "created_at"}
    assert expected.issubset(set(show_payload["data"].keys()))

    # ── Step 5: pause via CLI (CORE-07) ─────────────────────────────────────
    result = runner.invoke(app, ["--json", "sprint", "pause", sid, "--team", "e2e"])
    assert result.exit_code == 0, result.output
    pause_payload = json.loads(result.output)
    assert pause_payload["data"]["status"] == "paused"
    # state.json updated on disk
    raw = json.loads(state_path.read_text())
    assert raw["status"] == "paused"

    # ── Step 6: resume via CLI (CORE-07) ────────────────────────────────────
    result = runner.invoke(app, ["--json", "sprint", "resume", sid, "--team", "e2e"])
    assert result.exit_code == 0, result.output
    resume_payload = json.loads(result.output)
    assert resume_payload["data"]["status"] == "running"

    # ── Step 7: list via CLI (UX-04) ────────────────────────────────────────
    result = runner.invoke(app, ["--json", "sprint", "list", "--team", "e2e"])
    assert result.exit_code == 0, result.output
    list_payload = json.loads(result.output)
    assert list_payload["ok"] is True
    ids = [s["sprint_id"] for s in list_payload["data"]["sprints"]]
    assert sid in ids

    # ── Step 8: JSON envelope uniform across all 5 read-mode commands (UX-09) ──
    for cmd in [
        ["--json", "sprint", "status", sid, "--team", "e2e"],
        ["--json", "sprint", "show", sid, "--team", "e2e"],
        ["--json", "sprint", "list", "--team", "e2e"],
    ]:
        r = runner.invoke(app, cmd)
        p = json.loads(r.output)
        assert set(p.keys()) == {"ok", "data", "warnings", "error"}, f"cmd={cmd}"


def test_freeze_registry_exempts_sprint_internal_paths(e2e_env):
    """SAFETY-02 + Pitfall #5: freezing '/' does NOT prevent state.json writes."""
    from clawteam.cli.commands import app
    from clawteam.harness.freeze_registry import FreezeRegistry

    # Start sprint
    result = runner.invoke(app, ["--json", "sprint", "start",
                                 "--team", "e2e", "--goal", "test"])
    sid = json.loads(result.output)["data"]["id"]

    # Freeze the world
    reg = FreezeRegistry(team_name="e2e", sprint_id=sid)
    reg.freeze("/", agent="attacker", reason="attempted lockout", actor="test")

    # pause must still succeed — sprint-internal writes exempt
    result = runner.invoke(app, ["--json", "sprint", "pause", sid, "--team", "e2e"])
    assert result.exit_code == 0, result.output
    payload = json.loads(result.output)
    assert payload["data"]["status"] == "paused"


def test_evidence_gate_present_but_permissive_for_empty_artifacts(e2e_env):
    """SPRINT-02 / QUALITY-08: EvidenceGate integrated into advance flow.

    Phase 2 ships the gate; Phase 3 registers the schemas. With no artifacts,
    the presence check (inherited from ArtifactRequiredGate) passes trivially
    when artifact_names is empty (Plan 02-11 `_build_gate_chain` uses
    state.artifacts.keys() as default).
    """
    from clawteam.cli.commands import app
    from clawteam.sprint.conductor import SprintConductor

    result = runner.invoke(app, ["--json", "sprint", "start",
                                 "--team", "e2e", "--goal", "test"])
    sid = json.loads(result.output)["data"]["id"]

    # Direct conductor call — advance should succeed or return specific reason.
    # We do NOT advance in the CLI (no CLI for advance in Phase 2; Phase 4 may add).
    # This test verifies the gate chain does not crash with empty artifacts.
    c = SprintConductor(team_name="e2e")
    ok, reason = c.advance_phase(sid)
    # ok is True (empty artifacts pass presence check + forced_progress_gate sees 0 turns)
    # OR ok is False with a specific reason — either way, no exception raised.
    assert isinstance(ok, bool)
    assert isinstance(reason, str)


def test_ambiguous_prefix_surfaces_candidates(e2e_env, monkeypatch):
    """D-25: Ambiguous prefix → AMBIGUOUS_SPRINT error with candidates listed."""
    from clawteam.cli.commands import app

    # Force two sprints with shared prefix via uuid monkey-patch
    import uuid as uuid_mod
    forged = iter(["abcd1111", "abcd2222"])
    def fake_uuid4():
        try:
            return type("U", (), {"hex": next(forged) + "ffffffff"})()
        except StopIteration:
            return uuid_mod.UUID(int=0)
    monkeypatch.setattr(uuid_mod, "uuid4", fake_uuid4)

    runner.invoke(app, ["--json", "sprint", "start", "--team", "e2e", "--goal", "a"])
    runner.invoke(app, ["--json", "sprint", "start", "--team", "e2e", "--goal", "b"])

    result = runner.invoke(app, ["--json", "sprint", "status", "abcd", "--team", "e2e"])
    assert result.exit_code != 0
    payload = json.loads(result.output.strip().splitlines()[-1])
    assert payload["ok"] is False
    assert payload["error"]["code"] == "AMBIGUOUS_SPRINT"
    # candidates include both full ids
    assert "abcd1111" in payload["error"]["message"]
    assert "abcd2222" in payload["error"]["message"]
```

Run `pytest tests/test_phase2_integration.py -q` — expect all tests to pass (they exercise already-merged code from Plans 02-01..02-12).

If any test fails, identify the offending plan and fix at source — this plan does not
add production code, only a smoke test. Commit:
`test(02-13): Phase 2 end-to-end integration smoke test`
  </action>
  <verify>
    <automated>pytest tests/test_phase2_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase2_integration.py exists with 4 test functions.
    - `grep -c "def test_" tests/test_phase2_integration.py` == 4.
    - `grep -c "test_full_sprint_lifecycle_end_to_end\|test_freeze_registry_exempts_sprint_internal_paths\|test_evidence_gate_present_but_permissive_for_empty_artifacts\|test_ambiguous_prefix_surfaces_candidates" tests/test_phase2_integration.py` == 4.
    - `pytest tests/test_phase2_integration.py -q` exits 0.
    - Ruff clean.
    - Commit starts with `test(02-13):`.
  </acceptance_criteria>
  <done>End-to-end integration test covers all 5 major Phase 2 primitives; 4 tests pass; no production code edits.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 0 regression matrix hard-green gate (BLOCKING)</name>
  <files>tests/test_template_regression_matrix.py</files>
  <read_first>
    - tests/test_template_regression_matrix.py (Phase 0 Plan 00-04 shipped — do NOT edit without strong reason)
    - .planning/phases/00-foundation-upstream-rfc/00-04-SUMMARY.md if present (baseline 12-case expectations; if absent, the test file itself is self-documenting via parametrize)
    - .planning/ROADMAP.md §Phase 2 Success Criteria #10 (explicit BC invariant)
    - .planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-CONTEXT.md §Phase Boundary "Phase 2 must keep the Phase 0 regression matrix green"
  </read_first>
  <action>
Run the Phase 0 regression matrix against every modified file shipped by Phase 2:

```bash
pytest tests/test_template_regression_matrix.py -v 2>&1 | tee /tmp/phase2-regression.log
```

Expected output:
- Every parametrized case prefixed with `test_template_spawn_and_advance[...]` PASSED.
- Total: 12 passed, 0 failed, 0 errors.
- Exit code 0.

If ANY case fails, do the following (NOT edits to the regression matrix file):
1. Identify the FIRST failing template + the FIRST failing assertion.
2. Read the failure trace — it will implicate a specific Phase 2 file or hook site.
3. Map the failure to the responsible Phase 2 plan:
   - `TeamMessage` construction breaking → Plan 02-02 (ensure envelope fields are OPTIONAL)
   - `ArtifactStore.write` failing → Plan 02-06 (ensure frontmatter is NOT parsed at write)
   - Event subscriber interference → Plan 02-10 (ensure subscribers register only via SprintConductor)
   - `DefaultRoutingPolicy` behavior change → Plan 02-08 (ensure cycle detector adds suppression without changing existing throttle paths)
   - Plugin hook change → Plan 02-01 (ensure `contribute_evidence_schemas` has `return {}` default)
4. Fix at the source plan, NOT in the regression matrix file.
5. Re-run the matrix until green.
6. Document the fix in the relevant plan's SUMMARY.md.

If the regression matrix was already modified to accommodate Phase 2 (it should NOT
have been — this is a red flag), revert those changes: Phase 2's contract is that
existing template behavior is preserved byte-compatibly. The regression matrix is the
evidence of that contract.

Record the result:
```bash
pytest tests/test_template_regression_matrix.py -v 2>&1 | tail -20 > /tmp/phase2-regression-final.log
```

This task does NOT commit anything — it runs the matrix and confirms green.
  </action>
  <verify>
    <automated>pytest tests/test_template_regression_matrix.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_template_regression_matrix.py -q` exits 0.
    - Output shows `12 passed` (or equivalent — verify the number matches the Phase 0 baseline).
    - Zero `FAILED` lines.
    - Zero `ERROR` lines.
    - No edits to tests/test_template_regression_matrix.py (if any are found, revert and fix at the Phase 2 source plan instead).
  </acceptance_criteria>
  <done>Phase 0 regression matrix 12/12 green; SC#10 invariant held; upstream-PR viability gate passed.</done>
</task>

<task type="auto">
  <name>Task 3: Full pytest suite green (all Phase 0 + Phase 1 + Phase 2 tests)</name>
  <files></files>
  <read_first>
    - .planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-VALIDATION.md §Full suite command (`pytest tests/ -q`)
  </read_first>
  <action>
Run the full suite:

```bash
pytest tests/ -q 2>&1 | tee /tmp/phase2-full-suite.log
```

Expected:
- All Phase 0 tests green (env scrub, doctor, model profile, regression matrix, RFC lint).
- All Phase 1 tests green (PhaseRegistry, SprintState, InteractionGate, Q&A schema, HarnessOrchestrator wiring).
- All Phase 2 tests green — every new test file from Plans 02-01..02-12 passes:
  - tests/test_event_types_phase2.py (10 tests from Plan 02-01)
  - tests/test_plugin_hooks.py (Plan 01-01 base + Plan 02-01 additions)
  - tests/test_turn_envelope.py (12 tests from Plan 02-02)
  - tests/test_sprint_state.py (Phase 1 base + Plan 02-03 + Plan 02-11 additions)
  - tests/test_evidence_schemas.py (8 tests from Plan 02-04)
  - tests/test_freeze_registry.py (10 tests from Plan 02-05)
  - tests/test_artifact_caps.py (8 tests from Plan 02-06)
  - tests/test_evidence_gate.py (13 tests from Plan 02-07)
  - tests/test_cycle_detector.py (7 tests from Plan 02-08)
  - tests/test_transport_envelope.py (6 tests from Plan 02-08)
  - tests/test_forced_progress_gate.py (9 tests from Plan 02-09)
  - tests/test_safety_rails.py (14 tests from Plans 02-10)
  - tests/test_before_events.py (6 tests from Plan 02-10)
  - tests/test_sprint_conductor.py (20 tests from Plans 02-11 + 02-12)
  - tests/test_sprint_cli.py (15 tests from Plan 02-12)
  - tests/test_phase2_integration.py (4 tests from this plan)

Expected new-test total: 142+ tests.

If anything fails:
1. Identify the first failing test.
2. Map to the responsible Phase 2 plan.
3. Fix at source.
4. Re-run until suite green.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0.
    - Zero `FAILED` / `ERROR` lines.
    - New-test count ≥ 140 (sum of Phase 2 plan contributions).
    - Total suite runtime < 5 minutes (sanity bound; no hangs from freeze/cycle/forced_progress infinite-loop bugs).
  </acceptance_criteria>
  <done>Full suite green across Phase 0 + Phase 1 + Phase 2.</done>
</task>

<task type="auto">
  <name>Task 4: Phase 2 REQ-ID traceability audit</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (canonical REQ-ID list for Phase 2: CORE-05, CORE-07, INT-06, SPRINT-01, SPRINT-02, SKILL-09, SAFETY-01..04, QUALITY-01, QUALITY-02, QUALITY-03, QUALITY-06, QUALITY-08, QUALITY-11, UX-02..05, UX-09 — 21 IDs)
    - Every Plan 02-NN-PLAN.md frontmatter `requirements_addressed` field
  </read_first>
  <action>
Run a traceability grep across Phase 2 plans:

```bash
for req in CORE-05 CORE-07 INT-06 SPRINT-01 SPRINT-02 SKILL-09 \
           SAFETY-01 SAFETY-02 SAFETY-03 SAFETY-04 \
           QUALITY-01 QUALITY-02 QUALITY-03 QUALITY-06 QUALITY-08 QUALITY-11 \
           UX-02 UX-03 UX-04 UX-05 UX-09; do
    count=$(grep -l "$req" .planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-*-PLAN.md | wc -l)
    echo "$req covered by $count plan(s)"
done
```

Expected: every REQ-ID covered by ≥ 1 plan. If any REQ-ID shows 0 plans, STOP — a
requirement is orphaned. Identify the responsible plan retroactively (usually the plan
whose subsystem implements the primitive) and add the REQ-ID to its `requirements_addressed`
frontmatter.

Also confirm the Phase 2 REQ-IDs flow through to test files:
- CORE-05 → test_sprint_conductor.py (SprintConductor basic ops)
- CORE-07 → test_sprint_conductor.py (pause/resume subprocess test)
- INT-06 → test_sprint_conductor.py (auto_advance semantics)
- SPRINT-01 → test_plugin_hooks.py (contribute_evidence_schemas)
- SPRINT-02 → test_evidence_gate.py (4-check protocol)
- SKILL-09 → test_turn_envelope.py + test_transport_envelope.py (envelope enforcement)
- SAFETY-01 → test_safety_rails.py (/careful regex)
- SAFETY-02 → test_freeze_registry.py + test_safety_rails.py (/freeze path lock)
- SAFETY-03 → test_safety_rails.py (/guard composite)
- SAFETY-04 → test_freeze_registry.py (audit JSONL)
- QUALITY-01 → test_turn_envelope.py + test_transport_envelope.py
- QUALITY-02 → test_cycle_detector.py
- QUALITY-03 → test_artifact_caps.py + test_evidence_gate.py (per-phase cap)
- QUALITY-06 → test_freeze_registry.py (concurrent writer) + test_sprint_state.py
- QUALITY-08 → test_evidence_gate.py (frontmatter + stub + test-rerun)
- QUALITY-11 → test_forced_progress_gate.py
- UX-02 → test_sprint_cli.py (sprint start)
- UX-03 → test_sprint_cli.py (sprint status)
- UX-04 → test_sprint_cli.py (sprint list)
- UX-05 → test_sprint_cli.py (sprint show)
- UX-09 → test_sprint_cli.py (--json envelope uniformity)

Record the traceability matrix in 02-13-SUMMARY.md; flag any gap.
  </action>
  <verify>
    <automated>for req in CORE-05 CORE-07 INT-06 SPRINT-01 SPRINT-02 SKILL-09 SAFETY-01 SAFETY-02 SAFETY-03 SAFETY-04 QUALITY-01 QUALITY-02 QUALITY-03 QUALITY-06 QUALITY-08 QUALITY-11 UX-02 UX-03 UX-04 UX-05 UX-09; do count=$(grep -l "$req" .planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-*-PLAN.md 2>/dev/null | wc -l); [ "$count" -eq 0 ] && echo "ORPHAN: $req"; done; echo "audit complete"</automated>
  </verify>
  <acceptance_criteria>
    - Traceability grep output contains no "ORPHAN:" lines.
    - Every one of the 21 Phase 2 REQ-IDs is covered by at least one plan.
    - At least one test file per REQ-ID has a passing test exercising that requirement.
    - 02-13-SUMMARY.md records the traceability matrix with test-file citations.
  </acceptance_criteria>
  <done>All 21 Phase 2 REQ-IDs traced to plans + tests; no orphans.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human-verify Phase 2 ship criteria</name>
  <what-built>
Phase 2 is feature-complete. All 12 plans (02-01..02-12) have shipped, all 140+ new
tests pass, the Phase 0 regression matrix is 12/12 green, and all 21 Phase 2 REQ-IDs
have traceable test coverage.

The user now verifies, by reading the SUMMARY chain and running the CLI against a
fresh fixture, that the 10 ROADMAP §Phase 2 Success Criteria are functionally met.
  </what-built>
  <how-to-verify>
    Step 1 — Read the Phase 2 SUMMARY chain:
      - `cat .planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-{01..13}-SUMMARY.md | less`
      Confirm each plan's summary reports: files shipped, tests added, Phase 0 regression green.

    Step 2 — Run the full suite one more time from a clean checkout:
      ```bash
      pytest tests/ -q
      ```
      Confirm zero failures; record the test count.

    Step 3 — CLI smoke test against a scratch fixture:
      ```bash
      export CLAWTEAM_DATA_DIR=/tmp/phase2-smoke-$$
      mkdir -p $CLAWTEAM_DATA_DIR
      clawteam --json sprint start --team smoke --goal "Phase 2 ship verification" | tee /tmp/start.json
      SID=$(jq -r .data.id /tmp/start.json)
      clawteam --json sprint status $SID --team smoke | jq
      clawteam --json sprint show $SID --team smoke | jq
      clawteam --json sprint pause $SID --team smoke | jq
      clawteam --json sprint resume $SID --team smoke | jq
      clawteam --json sprint list --team smoke | jq
      ```
      Expected: every command emits `{ok: true, data: {...}, warnings: [], error: null}`.
      Phase transitions visible; pause/resume round-trips cleanly.

    Step 4 — ROADMAP §Phase 2 Success Criteria checklist — confirm each of the 10 items:
      1. [ ] `clawteam sprint start --team <name> --goal "..."` works; state.json exists; status/show/list/pause/resume all function; every command supports `--json`. (verified in Step 3)
      2. [ ] EvidenceGate validates frontmatter + required sections + stub detection + test-rerun hash cache. (verified by test_evidence_gate.py 13 tests)
      3. [ ] `clawteam sprint pause <id>` checkpoints; `clawteam sprint resume <id>` rehydrates across process restart. (verified by test_sprint_conductor.py::test_resume_after_process_restart)
      4. [ ] Every agent turn emits structured envelope; 8+ consecutive malformed → DriftRegression. (verified by test_transport_envelope.py)
      5. [ ] Cycle detector breaks A↔B after 3+ round-trips with same topic. (verified by test_cycle_detector.py)
      6. [ ] 2 consecutive no-progress turns per-agent trigger forced_progress_gate. (verified by test_forced_progress_gate.py)
      7. [ ] Artifact-delta-per-hour + token-per-useful-byte are exposed — **Phase 2 ships the turn_counters + artifact-bytes primitives; metric surfacing into `sprint status` JSON is Phase 7 dashboard scope**. Flag as partial coverage with Phase 7 follow-up.
      8. [ ] /careful, /freeze, /guard, /unfreeze registered as EventBus hooks + CLI sub-app. (verified by test_safety_rails.py + test_before_events.py)
      9. [ ] Artifact size caps enforced (50 KB per-file, 500 KB per-phase). (verified by test_artifact_caps.py + test_evidence_gate.py)
      10. [ ] Phase 0 regression matrix 12/12 green. (verified by Task 2)

    Step 5 — If any of the 10 items is not functionally met, identify the responsible plan and
    either (a) re-open the plan to fix, or (b) explicitly document the gap + Phase 7 follow-up
    in Phase 2 SUMMARY + ROADMAP. SC#7's metric surfacing is known-partial (Phase 7 dashboard);
    all 9 other items are Phase 2 ship-gate.
  </how-to-verify>
  <resume-signal>
    Type "approved — Phase 2 ready to merge" OR describe any gap to re-plan.

    If approved:
      - Phase 2 commit: `feat(phase-02): ship sprint engine + evidence gates + theater/drift/deadlock prevention`
      - Update .planning/STATE.md: current_phase="02-complete", next="03-gstack-team-template-methodology-port"
      - Update .planning/ROADMAP.md §Phase 2 checkbox to [x]
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 aggregate surface → downstream users | every primitive shipped by Plans 02-01..02-12 is visible here; a regression matrix failure indicates a trust-boundary violation between Phase 2 and pre-existing ClawTeam users |
| test_phase2_integration.py → Phase 2 subsystems | integration test exercises code that could have unknown interaction bugs; failures here are load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28 (ship-gate) | BC/integration regression | all Phase 2 primitives together | mitigate | Regression matrix 12/12 green + end-to-end test 4/4 green + REQ-ID traceability 21/21 — three independent gates must all pass before ship. Any failure routes back to the source plan for fix, not masked here. |
| T-02-29 | I (information leak via integration test logs) | test_phase2_integration.py | accept | Tests use ephemeral tmp_path; no secrets embedded in fixtures. Test output may appear in CI logs — standard pytest exposure, already covered by CI policy. |

All other Phase 2 threats (T-02-01 SSRF via deploy_url, T-02-02 path bypass, T-02-03
test-command exec, T-02-04 YAML inject, T-02-05 JSONL tamper, T-02-06 topic-hash DoS,
T-02-07 careful bypass, T-02-08..T-02-29 variants) are handled at their owning plans.
This plan does not introduce new attack surface — it exercises the existing surface
end-to-end and runs the BC regression gate.

## Residual High-Severity Risks (forwarded to later phases)

- **T-02-01 SSRF** — Phase 5 `/ship` must wire an allowlist checker around the
  `deploy_url_checker` injection point in EvidenceGate. Phase 2 ships the seam.
- **T-02-22 turn_id gaming** — Agent can game the theater-detector counter with
  constant turn_ids. Phase 2 accepts via the artifact-bytes fallback; full mitigation
  via LLM-based turn verification is v1.x scope.

</threat_model>

<verification>
1. `pytest tests/test_phase2_integration.py -q` → 4 passed (Task 1 gate).
2. `pytest tests/test_template_regression_matrix.py -q` → 12 passed (Task 2 gate — BLOCKING).
3. `pytest tests/ -q` → zero failures across Phase 0 + Phase 1 + Phase 2 (Task 3 gate).
4. REQ-ID traceability audit (Task 4) reports zero orphans.
5. Human-verify checkpoint (Task 5) returns "approved".
</verification>

<success_criteria>
- Phase 0 regression matrix 12/12 green — SC#10 invariant held, upstream-PR viability confirmed.
- Full pytest suite green; new-test count ≥ 140.
- All 21 Phase 2 REQ-IDs traced to plans + tests with zero orphans.
- End-to-end integration test covers sprint lifecycle across all Phase 2 primitives.
- Human-verify checkpoint approves Phase 2 ship.
- ROADMAP §Phase 2 checklist items 1-6, 8-10 functionally met; item 7 (metric surfacing in sprint status) explicitly flagged as Phase 7 follow-up (partial coverage documented, not a ship-block).
</success_criteria>

<output>
After completion, create `.planning/phases/02-sprint-engine-evidence-gates-theater-drift-deadlock-prevention/02-13-SUMMARY.md` capturing:
- Full REQ-ID traceability matrix (21 IDs × plans × test files).
- Phase 0 regression matrix final status (12/12 with timestamp).
- Full suite test count + runtime.
- Human-verify checkpoint approval text.
- Flagged Phase 7 follow-up: SC#7 `artifact_delta_per_hour` + `token_per_useful_byte` surfaced in `sprint status` dashboard.
- Flagged upstream-PR readiness: Phase 0 + Phase 1 + Phase 2 form the target upstream bundle per ROADMAP §Upstream-PR-scope; UP-01 v1-post-ship confirmed.
</output>
